# Telegram Bot — create via @BotFather
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Vercel KV — auto-populated when you link a KV store in the Vercel dashboard
# (Settings → Storage → Connect KV Database)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: set to "true" to enable verbose HTML-excerpt logging
DEBUG_MODE=false
